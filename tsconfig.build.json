{
    "extends": "./tsconfig",
    "compilerOptions": {
        "types": ["node"] /* Type declaration files to be included in compilation. */
    },
    "exclude": ["node_modules", "typings", "build", "tests"]
}
